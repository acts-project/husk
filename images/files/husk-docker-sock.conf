# Runner hardcodes /var/run/docker.sock; point it at the rootless podman user
# socket. tmpfiles.d so it survives reboot (/run is tmpfs).
L+ /run/docker.sock - - - - /run/user/1000/podman/podman.sock
