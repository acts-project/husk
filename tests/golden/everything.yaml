#cloud-config
# Golden image: everything slow and static is in the image already, so this is
# intentionally minimal. See render_cloud_init.

write_files:
  - path: /var/lib/husk/jitconfig
    permissions: '0600'
    content: "JITBLOB"
  # Bias the kernel OOM killer away from the runner agent and toward a runaway
  # job. Job containers run under rootless podman in user-1000.slice — a cgroup
  # distinct from this service — so protecting the agent here makes a memory-hungry
  # job the victim, not the process supervising it. -900 is strong protection
  # short of -1000's outright exemption (which would also shield an agent that
  # itself leaked). Unconditional: always safe. The daemon-reload below (before the
  # runner starts) picks it up; the unit is started fresh each cycle regardless.
  - path: /etc/systemd/system/husk-runner.service.d/10-oom.conf
    content: |
      [Service]
      OOMScoreAdjust=-900
  # Environment for every job container (see [pool.container] env). The
  # image is shared with GitHub-hosted runners, so fleet-specific facts
  # arrive as environment, not as image content.
  - path: /etc/containers/containers.conf.d/20-env.conf
    content: |
      [containers]
      env = ["TERM=xterm", "APT_MIRROR=http://mirror.cern.ch"]
  # Hard memory ceiling for the job. Every job container is a rootless-podman
  # scope nested under user-1000.slice, so a cap on that slice bounds the whole
  # job: an overshoot triggers a memcg OOM confined to the job's own cgroup
  # (podman reports OOMKilled, the step fails cleanly) rather than the kernel
  # choosing a victim across the VM. user-1000.slice is already up via linger and
  # job scopes are created later, so the daemon-reload below applies the cap in
  # time. Caps the AGGREGATE of the slice — right for one-job-per-slot; a job's
  # own service containers share the ceiling.
  - path: /etc/systemd/system/user-1000.slice.d/10-memmax.conf
    content: |
      [Slice]
      MemoryMax=6G
  # CernVM-FS client config (client + autofs are baked; this is the
  # per-pool dynamic layer: proxy, repo list, cache quota).
  #
  # CVMFS_USE_GEOAPI=no overrides the CERN domain config's default of yes. Geo
  # sorts the 7-server CVMFS_SERVER_URL list by distance via a per-mount round
  # trip — worthless here (every slot in a pool is co-located behind a local
  # Squid fronting the CERN Stratum-1, which is already first in that list) and
  # observed to stall cold mounts ~20s each when the geo query times out (it is
  # not counted as an I/O error). The failover list is kept, just not re-sorted.
  - path: /etc/cvmfs/default.local
    content: |
      CVMFS_HTTP_PROXY="http://squid.cern.ch:3128"
      CVMFS_REPOSITORIES=sft.cern.ch
      CVMFS_QUOTA_LIMIT=8000
      CVMFS_USE_GEOAPI=no

  # Default per-repo bind mounts for every job container. podman applies
  # containers.conf.d to BOTH the CLI shim and the API socket, so this
  # covers `container:` jobs and step-level `docker run` alike. Per-repo, not a
  # whole-/cvmfs bind — the autofs root readdir is denied under the rootless
  # userns, but a bind of an already-mounted repo tree is not.
  - path: /etc/containers/containers.conf.d/10-cvmfs.conf
    content: |
      [containers]
      volumes = ["/cvmfs/sft.cern.ch:/cvmfs/sft.cern.ch"]

  # Coarse egress firewall ruleset — the tunable security policy, kept in
  # cloud-init (NOT baked) so a pool can change it without rebuilding the image.
  - path: /etc/nftables/husk-egress.nft
    content: |
      #!/usr/sbin/nft -f
      table inet husk {}
      delete table inet husk

      table inet husk {
        set cvmfs_proxy { type ipv4_addr; }
        set egress_allow { type ipv4_addr; }
        chain output {
          type filter hook output priority 0; policy accept;

          oif "lo" accept
          ct state established,related accept

          # Keep name resolution + time sync working: CERN's own resolvers/NTP
          # live inside the blocked ranges, so allow 53/123 to anywhere first.
          udp dport { 53, 123 } accept
          tcp dport 53 accept

          # Allow the CVMFS HTTP proxy. Its CERN squids live in the
          # CERN-internal ranges dropped below, so this accept must
          # precede them; the set is populated in runcmd after an
          # in-guest resolve of the proxy hostnames.
          ip daddr @cvmfs_proxy accept

          # Allow the operator-listed hosts. They live in the
          # CERN-internal ranges dropped below, so this accept must
          # precede them; the set is populated in runcmd after an
          # in-guest resolve.
          ip daddr @egress_allow accept

          # Deny all other egress to CERN-internal networks. Public internet
          # falls through to `policy accept`. Extend these sets as needed.
          ip daddr { 128.141.0.0/16, 128.142.0.0/16, 137.138.0.0/16, 188.184.0.0/15 } drop
          ip6 daddr { 2001:1458::/32, 2001:1459::/32 } drop
        }
        chain input {
          type filter hook input priority 0; policy accept;

          iif "lo" tcp dport 9100 drop
          tcp dport 9100 ip saddr { 192.168.122.1/32 } accept
          tcp dport 9100 drop
        }
      }

runcmd:
  # jitconfig is written root-owned above; the runner service runs as `runner`.
  # /run/husk holds the timing markers below and is deliberately NOT chowned:
  # it must stay unwritable by the (untrusted) job, which runs as `runner`.
  - mkdir -p /var/lib/husk /run/husk
  - chown -R runner:runner /var/lib/husk
  - read -r t _ < /proc/uptime; echo "runcmd_start $t" >> /run/husk/timings
  # GPU runtime activation (every boot — the kmod load + CDI spec are
  # hardware-dependent and cannot be baked into the image).
  - modprobe nvidia
  - nvidia-ctk cdi generate --output=/etc/cdi/nvidia.yaml
  - read -r t _ < /proc/uptime; echo "gpu_activated $t" >> /run/husk/timings
  # Lock down egress just before the (untrusted) runner starts.
  - /usr/sbin/nft -f /etc/nftables/husk-egress.nft
  - read -r t _ < /proc/uptime; echo "firewall_applied $t" >> /run/husk/timings
  # Open the firewall to the allowlisted hosts (CERN package mirrors and
  # the like): resolve in-guest, since these are load-balanced aliases
  # whose addresses rotate, and add the IPs to the nft set. Soft failure —
  # an unresolvable host opens no hole and the slot still boots.
  - |
    ips=$(getent ahostsv4 linuxsoft.cern.ch | awk '{print $1}' | sort -u | paste -sd, -)
    [ -n "$ips" ] && nft add element inet husk egress_allow "{ $ips }"
  - read -r t _ < /proc/uptime; echo "egress_resolved $t" >> /run/husk/timings
  # Open the firewall to the CVMFS proxy: resolve the squid hostnames
  # in-guest (DNS/53 stays allowed post-lockdown) and add their IPs to
  # the nft set, so rotating A-records self-heal on every recycle.
  - |
    ips=$(getent ahostsv4 squid.cern.ch | awk '{print $1}' | sort -u | paste -sd, -)
    [ -n "$ips" ] && nft add element inet husk cvmfs_proxy "{ $ips }"
  - read -r t _ < /proc/uptime; echo "cvmfs_proxy_resolved $t" >> /run/husk/timings
  # Eager-mount the configured repos so a per-repo bind makes them visible
  # inside rootless job containers (autofs triggers don't cross the userns).
  # Probe every repo CONCURRENTLY and wait: each probe is an independent
  # network mount (~20s cold), so serial would sum them onto the critical path
  # before the runner starts. Each subshell stamps its own completion marker —
  # a short line via O_APPEND, atomic under PIPE_BUF, so the parallel writes
  # don't interleave. Because they all start together, a repo's cost is its
  # marker minus cvmfs_proxy_resolved; cvmfs_mounted (after wait) is the total.
  - systemctl start autofs
  - |
    for r in sft.cern.ch; do
      ( cvmfs_config probe "$r" || true
        read -r t _ < /proc/uptime; echo "cvmfs_probe_$r $t" >> /run/husk/timings ) &
    done
    wait
  - read -r t _ < /proc/uptime; echo "cvmfs_mounted $t" >> /run/husk/timings
  # The runner unit is baked but NOT enabled for boot; cloud-init starts it each
  # cycle once the fresh JIT config is in place (Type=simple returns immediately).
  - systemctl daemon-reload
  # node_exporter is baked but NOT enabled for boot, and is started only AFTER
  # the ruleset is applied — so :9100 is never briefly open to the world. Before
  # the runner, so metrics are live for the whole job.
  - systemctl start husk-node-exporter.service
  - read -r t _ < /proc/uptime; echo "exporter_started $t" >> /run/husk/timings
  - systemctl start husk-runner.service
  - read -r t _ < /proc/uptime; echo "runner_started $t" >> /run/husk/timings
  # Boot-timing report (baked oneshot, ordered After the runner so it never
  # delays registration). It turns the markers above into the console block and
  # into node_exporter's textfile .prom — ALL of the expensive analysis happens
  # there, after the runner is already registering, never here. `--no-block` so
  # it can't hold up the final stage; `|| true` — diagnostics must never fail
  # the boot. Started last, so the marker file is complete when it reads it.
  - systemctl start --no-block husk-bootreport.service || true
  # Belt-and-suspenders wall-clock cap (runner is unprivileged -> real net).
  - shutdown -h +360
