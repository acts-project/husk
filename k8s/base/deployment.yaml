apiVersion: apps/v1
kind: Deployment
metadata:
  name: huskd
spec:
  # huskd serves its HTTP surface as soon as the process is up, but only ONE pod
  # reconciles at a time: it blocks on an flock at controller.lock_path
  # (src/husk/lock.py, wait_acquire) before touching any backend. Contention is a
  # handoff, not an error — a second pod serves a read-only "standby" dashboard
  # until the lock frees, then takes over reconciling.
  #
  # The base default is Recreate because the `run` lock volume here is a per-pod
  # emptyDir: two overlapping pods would each see their OWN empty lock and both
  # reconcile. An overlay that puts the lock on a SHARED volume (see overlays/cern,
  # RWX on CephFS) can safely switch to RollingUpdate(maxUnavailable:0) — the new
  # pod serves immediately and waits out the old pod's lock, so the dashboard never
  # goes dark across a deploy; only reconcile pauses for the handoff.
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: huskd
  template:
    metadata:
      labels:
        app: huskd
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9100"
        prometheus.io/path: /metrics
    spec:
      # On SIGTERM the daemon stops accepting, drains its in-flight reconcile tick,
      # flushes metrics-state, then releases the controller lock — in that order
      # (see cli._activate). Give it room to finish: under a shared-lock rolling
      # update the successor can't reconcile until this release lands, and a SIGKILL
      # here leaves the lock to be reclaimed only after the CephFS session times out.
      terminationGracePeriodSeconds: 45
      # Fail a bad ConfigMap BEFORE the daemon starts. `huskctl validate` parses
      # the config and touches nothing external — no cloud, no libvirt, no GitHub —
      # so this is fast and side-effect free, and the pod never reaches the point
      # of taking the controller lock with a config that can't work. A parse error
      # then shows up as Init:Error with the message in this container's logs,
      # instead of the main container crash-looping.
      #
      # `just k8s-validate` runs the identical check on your laptop.
      initContainers:
        - name: validate-config
          # MUST be the same build as the huskd container below: it validates with
          # the same models, which are extra="forbid", so an older validator
          # rejects a config key the daemon understands and crash-loops on a
          # perfectly good config. `oc set image` therefore pins BOTH containers
          # (see k8s-live-deploy) — pinning only huskd= leaves this one on the
          # :latest each node happens to have cached, and IfNotPresent means it
          # never refreshes, so the failure appears on some nodes and not others.
          image: ghcr.io/acts-project/husk:latest
          imagePullPolicy: IfNotPresent
          command: ["huskctl", "validate", "--config", "/etc/husk/config.toml"]
          env:
            # The loader insists on a key being present even though validate never
            # uses it, so the same secret is wired here.
            - name: HUSK_GITHUB__PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: huskd-github
                  key: private-key.pem
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: config
              mountPath: /etc/husk
              readOnly: true
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
      containers:
        - name: huskd
          # Published by .github/workflows/build-app-image.yml. Overlays retag this;
          # the live deploy pins a sha- tag over it via `oc set image`.
          image: ghcr.io/acts-project/husk:latest
          imagePullPolicy: IfNotPresent
          # Dockerfile's CMD already is ["--config", "/etc/husk/config.toml"];
          # repeated here so the mount path and the flag can't drift apart.
          args: ["--config", "/etc/husk/config.toml"]
          ports:
            - containerPort: 9100
              name: http
          env:
            # PEM contents, not a path. config.py resolves this BEFORE
            # [github].private_key_path, so the TOML carries no key material.
            - name: HUSK_GITHUB__PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: huskd-github
                  key: private-key.pem
            # Enables POST /webhook (which job each busy slot runs, plus queue
            # depth). `optional: true` because huskd is poll-driven and complete
            # without it: a cluster with no webhook-secret key in the Secret starts
            # normally with the endpoint disabled rather than crash-looping. Unset
            # does NOT mean "accept unsigned" — the route refuses every delivery
            # until a secret is configured. Not wired into the validate init
            # container above: it never serves HTTP, so the key is meaningless
            # there and would only widen where the secret is mounted.
            - name: HUSK_GITHUB__WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: huskd-github
                  key: webhook-secret
                  optional: true
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: config
              mountPath: /etc/husk
              readOnly: true
            # openstacksdk looks for clouds.yaml under $HOME/.config/openstack;
            # the image pins HOME=/app.
            - name: openstack
              mountPath: /app/.config/openstack
              readOnly: true
            # controller.image_cache_dir. Golden qcow2s land here before being
            # fanned out to Glance / libvirt hosts — multi-GB, so give it real
            # storage in any long-lived cluster (see overlays/cern).
            - name: image-cache
              mountPath: /app/.cache/husk/images
            # controller.lock_path lives here. In the base this is a per-pod
            # emptyDir (kernel drops the lock on crash), which is why the base is
            # Recreate. An overlay that wants a zero-downtime RollingUpdate swaps
            # this for a SHARED RWX claim so both pods contend on one flock — the
            # single-reconciler guarantee then comes from the lock, not from the
            # rollout strategy (see overlays/cern).
            - name: run
              mountPath: /run/husk
            # controller.metrics_state_path. huskd's accumulated counters and
            # histograms (failure rates, recycle-duration distributions) — a few KB
            # of JSON. emptyDir here means they reset on every pod restart, which is
            # fine locally; the cern overlay swaps in a PVC so a config change (which
            # always means a restart) doesn't silently truncate a 30d query window.
            - name: metrics-state
              mountPath: /var/lib/husk
            # SSH credential for libvirt pools. THREE consumers share it: libvirt's
            # own qemu+ssh:// transport (spawned by libvirt-python), the backend's
            # host-side qemu-img/genisoimage (libvirt_backend._ssh), and the guest
            # metrics scrape (guest_scrape, which curls the guest FROM the host).
            # All three are plain `ssh`, so all three read $HOME/.ssh — and the image
            # pins HOME=/app, so this one mount serves all of them.
            #
            # The key is named id_ed25519 deliberately: that is a default identity
            # filename, so nothing needs an IdentityFile directive. known_hosts must
            # be in the same Secret and PRE-SEEDED (ssh-keyscan -t ed25519 HOST) —
            # every caller passes BatchMode=yes and none overrides
            # StrictHostKeyChecking, so an unknown host key is a hard failure with no
            # prompt, surfacing as an opaque backend error rather than "unknown host".
            # The mount is read-only anyway, so ssh could not learn the key here.
            - name: ssh
              mountPath: /app/.ssh
              readOnly: true
          # All three probes point at /livez, never /healthz. /healthz reports
          # reconcile *freshness*, which is 503 until the first reconcile lands —
          # and a first reconcile that has to upload a golden image to Glance can
          # take longer than any sane startup budget. Wiring that to readiness took
          # the Service's only endpoint away (dashboard + /metrics 503 through the
          # ingress) precisely while huskd was doing its slowest legitimate work,
          # and wiring it to liveness/startup restarted the pod mid-upload, into the
          # same upload, forever. /livez answers as soon as the event loop does,
          # which is the only condition a restart or a re-route can actually fix.
          # Staleness is an alerting signal: scrape /healthz or alert on the
          # husk reconcile-age metric.
          livenessProbe:
            httpGet:
              path: /livez
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /livez
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /livez
              port: http
            periodSeconds: 15
            failureThreshold: 12
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
      volumes:
        - name: config
          configMap:
            name: huskd-config
        - name: openstack
          secret:
            secretName: huskd-openstack
        - name: image-cache
          emptyDir: {}
        - name: run
          emptyDir: {}
        # NB overlays patch volumes BY INDEX (JSON6902 — see overlays/cern). Append
        # new entries at the end; inserting one above shifts every later index and
        # silently repoints an existing patch at the wrong volume.
        - name: metrics-state
          emptyDir: {}
        - name: ssh
          secret:
            secretName: huskd-ssh
            # optional: an OpenStack-only deployment has no libvirt host to reach,
            # and without this the pod would refuse to start until the Secret exists
            # — i.e. adding this volume would break every current deployment. With
            # it, the mount is simply empty until `just k8s-secrets` supplies a key.
            optional: true
            # 0440, not 0400. The file is owned by ROOT with its group set from the
            # pod's fsGroup (explicit 999 in overlays/local; injected from the
            # project's range by OpenShift's restricted SCC at CERN), so the process
            # reads it via the group bit — 0400 root-owned would be unreadable by
            # any non-root uid, which is every uid we run as.
            #
            # This does NOT trip ssh's "permissions are too open" refusal: that check
            # (sshkey_perm_ok) only fires when the key file is owned by the CALLING
            # uid, and here it is owned by root. Were the pod ever to run as root,
            # 0440 would start failing — runAsNonRoot: true is what keeps that true.
            defaultMode: 0440
